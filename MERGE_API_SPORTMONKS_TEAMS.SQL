MERGE INTO API_SPORTMONKS_TEAMS AS Target
USING (VALUES (?, ?)) AS Source (TEAM_ID, TEAM_NAME)
ON Target.TEAM_ID = Source.TEAM_ID

WHEN MATCHED THEN 
    UPDATE SET TEAM_NAME = Source.TEAM_NAME

WHEN NOT MATCHED THEN 
    INSERT (TEAM_ID, TEAM_NAME) VALUES (Source.TEAM_ID, Source.TEAM_NAME);