IF NOT EXISTS (SELECT * FROM INFORMATION_SCHEMA.TABLES WHERE TABLE_NAME = 'API_SPORTMONKS_TEAMS')
BEGIN
    CREATE TABLE API_SPORTMONKS_TEAMS (
        ID INT IDENTITY(1,1),
        TEAM_ID INT NOT NULL PRIMARY KEY ,
        TEAM_NAME VARCHAR(100) NOT NULL
    )
END